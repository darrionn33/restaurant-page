import put from "put-selector";
export default function menu(params) {
  const menu = put("main#menu");
  const menuItem = put("div.menu-item");
  put(menuItem, "div", "Hamburger");
  put(
    menuItem,
    "div.dot-leaders",
    "................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................"
  );
  put(menuItem, "div", "50");
  put(menu, menuItem);
  return menu;
}
